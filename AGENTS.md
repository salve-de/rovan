<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

---

# Rovan プロジェクト AI開発者・エージェント最高行動憲法
（ALL AI AGENTS MUST OBEY THIS PROTOCOL）

本リポジトリで作業するすべてのAIエージェント（Cursor, Claude, Copilot, ChatGPT, Antigravity等）は、以下のプロジェクト原則を遵守する。オーナーの事業目標と、実装・本番稼働の証拠を区別する。

## 1. このプロジェクトで達成すること（What We Achieve）
- **目標**: 地域の中小企業や専門事業者が、大手と正面から資本力・即日対応を競わず、自社の実在するニッチな強みに合うAI購買相談で推薦候補を取り戻す。
- **提供する価値**: URLまたは社名を起点に、AI回答と公開情報を観測し、地域・専門分野・自社施工など参照元で確かめた事実をRovan上の公開情報参照ページへ整備する。許可された更新、再測定、完了報告まで継続する。加えて、人力ASO/LLMO/GEOコンサルの通常作業を、外部顧客サイトへの直接書き込みを除いて自動化し、実装可能な成果物まで生成する。
- **目標と証拠**: この目標を単なる診断・下書き提供へ縮小しない。一方、AIが必ず参照・推薦する、顧客を獲得した、全機能が実装・本番運用済みとは証拠なしに述べない。

## 2. ユーザーの手間を極限まで減らす（Zero Effort）
- 自社サイト改修ゼロ。ファイル設置、DNS変更、ブログ作成、質問票、週次の承認作業を通常利用の前提にしない。
- URLまたは社名だけで開始する。社名から始める場合の候補サイト確認と初回の公開・継続更新への同意は、短い導線にまとめる。
- 「競合が始めたので御社もできますか」と能力追加を求めず、取得済み公開情報の実在する強みから自動で進める。不足情報は推測で埋めない。
- 初回同意・対象確認や例外対応まで存在しないという意味で「完全放置」を保証しない。

## 3. 許可に基づく自動化と公開の境界
- 初回に、対象企業・Rovan上の公開先・参照元・更新する事実項目と操作範囲を示し、公開と継続自動更新への明示的なオプトインを得る。その同意の範囲内で、参照元付きの客観的事実の追加・更新・失効、再測定、報告を自動で続けることを製品目標とする。通常の週次更新に毎回の承認を要求しない。対象や公開先の変更、範囲外の内容、権利・根拠が未確認の内容は保留し、必要な範囲だけ追加確認する。同意撤回・停止・訂正・非公開の導線と、同意範囲、原典、公開差分、実行日時、失敗・復旧の記録を持つ。取得失敗だけで事実を削除せず、原典での失効と区別する。
- 顧客サイト、CMS、第三者ポータル、広告、CRM、AI Providerの内部データは変更しない。自動反映先は同意されたRovanの公開情報参照ページに限る。
- Change Packの自由文や範囲外の編集は下書きとして保留する。通常の事実更新まで恒久的な下書きだけに制限しない。
- 管理者の通常運用も自動化し、申請・更新代行など労働集約型サービスを前提にしない。障害時の監視・復旧・権利者対応は維持する。
- Google Maps評価・口コミは初版で転載しない。他者の文章・画像を無断転載せず、参照元のある客観的事実を扱う。公開情報であることだけで権利・規約の確認が不要とはしない。
- 本人確認や公的認証のないページを「公式台帳」「公認推薦」と呼ばない。AIの回答・推薦・順位・流入・問い合わせ・契約・売上を保証せず、訂正・非公開申請窓口を常備する。

## 4. 長期的視野・スケーラビリティ
- 実際の処理時間、再試行、レート制限、Provider費用、同時実行数、監視と復旧を測定してからインフラを選ぶ。
- 無料枠や短期の設定容易性だけを理由に、長時間のクロール・AI測定・再試行を短いリクエストへ詰め込まない。
- ジョブ、キュー、リース、冪等性、段階的な再開を使う場合は、実行ログと失敗時の復旧手順を残す。規模の数値や費用を根拠なく約束しない。

## 5. プロジェクトの北極星と測定モデル

北極星は **AI顧客奪還シェア（Competitor Take-Back Share）**。買い手を想定した固定代表50問パネルに対し、自社が推薦候補枠に入った質問の割合と、その導入前後・継続測定での推移を追う。競合候補との関係は回答ログで併記する。これは測定パネル上の観測であり、実際の顧客数・顧客シェア・売上・全利用者の会話・AI内部順位ではない。

算出は「自社の候補入りを確認した質問数 ÷ 取得に成功した固定パネル質問数」。全50問取得時の分母は50。欠損時は成功分母・予定50問・未取得数を併記し、部分観測として完全パネルと区別する。前後比較は両時点で比較可能な質問に揃え、その分母を示す。分母0や条件不一致は比較不可とする。質問・パネル版・AI面・Provider・モデル・地域・言語・反復条件と各測定日時を保存し、Provider別に表示する。反復の判定規則は事前に固定する。候補入りは競合の脱落や第一候補化を意味しない。第一候補への変化は回答ログで別途確認する。

初回候補外質問だけを分母にする「候補回復率」は補助指標であり、北極星の名称・分母を置き換えない。無料の短いパネルを50問の結果として表示せず、パネル移行時は基準測定を取り直す。改善だけでなく、低下・変化なし・欠損も示し、施策による因果効果や将来の推薦成果を保証しない。

AI回答、候補名、参照元URL、公開情報の差分を観測事実として保存する。競合サイト変更は保存した前後クロール差分がある場合だけ記録する。実装・本番の成立は、同意から自動公開、再測定、通知、撤回・復旧までの証拠で別途確認する。


## 6. 人力ASO/LLMO業務の無人化と外部書き込み禁止

詳細仕様は `docs/AUTONOMOUS_AI_SEARCH_SUPERSET_STRATEGY.md` をCURRENT/HARDとして扱う。

- Buyer Prompt生成、AI回答実測、競合・Citation/参照元分析、競合Web差分、Evidence Gap、技術監査、優先順位付け、Change Pack生成、再測定、監視、定期報告は通常ケースを人手なしで完結させる。
- Change Packは抽象的な助言で止めず、対象URL、変更位置、完成文、FAQ、比較表、title/description、JSON-LD、内部リンク案、根拠、対象Buyer Prompt、再測定条件まで実装直前の粒度を目指す。
- 顧客サイト、CMS、DNS、広告、CRM、第三者サイトへの直接変更は行わない。外部システムへの最終反映は顧客側の管理下に残す。
- 「直接編集しない」を理由に単発レポート製品へ後退させない。Rovan側の観測、診断、生成、優先順位、監視、再測定は自動化する。
- 機能追加で売上増加と運営者の手作業が比例する設計は避ける。例外対応以外の顧客別リサーチ、記事作成、週次判断、レポート作成を運営者の仕事にしない。
